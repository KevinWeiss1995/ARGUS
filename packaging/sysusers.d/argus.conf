# Create the argus system user for future non-root daemon operation.
# Currently argusd runs as root (eBPF requires CAP_SYS_ADMIN on <5.19),
# but the user is created for ambient-capability setups on 5.19+ kernels.
u argus - "ARGUS telemetry agent" /var/lib/argus /usr/sbin/nologin
